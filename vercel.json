{
  "framework": "fastapi",
  "region": "auto",
  "build": "pip install -r requirements.txt",
  "install": "pip install -r requirements.txt",
  "start": "python -m uvicorn app_vercel:app --host 0.0.0.0 --port $PORT",
  "frameworkToken": "",
  "python": {
    "runtime": "3.11"
  }
}